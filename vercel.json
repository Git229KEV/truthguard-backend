{
  "buildCommand": "cd rumor-detector && npm install && npm run build",
  "outputDirectory": "rumor-detector/dist",
  "installCommand": "cd rumor-detector && npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
